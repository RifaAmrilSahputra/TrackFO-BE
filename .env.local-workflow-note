Temporary note: DB host resolution issue.
Current .env uses DATABASE_URL host 'db' which is not resolvable/reachable from host machine.
Docker compose exposes MySQL on host:3306, so for local Prisma/test runs update DATABASE_URL to use 127.0.0.1/localhost.

